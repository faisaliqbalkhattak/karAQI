name: Forecast pipeline (hourly)

# Generates the pre-computed forecast JSON for the dashboard.
# Runs every hour at :05 (4 minutes after the feature pipeline at :01).
# Also triggered after the training pipeline completes.
# Commits static_forecast.json to karAQI-data repo.
#
# Reference AQI data comes from Open-Meteo (free, keyless, no rate limits).
# Our model predictions come from the trained Ridge model.

on:
  schedule:
    - cron: "04 * * * *"
  workflow_dispatch:
  workflow_run:
    workflows: ["Training pipeline (daily)"]
    types: [completed]

permissions:
  contents: write

jobs:
  generate-forecast:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: ${{ github.event.workflow_run == null || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-forecast.txt

      - name: Install forecast pipeline dependencies
        run: python -m pip install --upgrade pip && python -m pip install -r requirements-forecast.txt

      - name: Fetch latest Open-Meteo data (incremental, keyless)
        run: python -m src.build_features --fetch --incremental

      - name: Backfill feature store
        run: python -m src.feature_store backfill-hourly --replace

      - name: Fetch trained model from karAQI-data
        env:
          DATA_REPO_TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
        run: |
          if [ -z "$DATA_REPO_TOKEN" ]; then
            echo "ERROR: DATA_REPO_TOKEN not set. Cannot fetch model."
            exit 1
          fi
          git clone https://oauth2:${DATA_REPO_TOKEN}@github.com/faisaliqbalkhattak/karAQI-data.git /tmp/karAQI-data || {
            echo "ERROR: Failed to clone karAQI-data. Check DATA_REPO_TOKEN."
            exit 1
          }
          mkdir -p models
          cp /tmp/karAQI-data/models/aqi_forecast_hourly_ridge.joblib models/ 2>/dev/null || true
          cp /tmp/karAQI-data/models/aqi_forecast_hourly_xgb.joblib models/ 2>/dev/null || true
          cp /tmp/karAQI-data/models/aqi_forecast_hourly_models.json models/ 2>/dev/null || true
          if [ ! -f models/aqi_forecast_hourly_models.json ]; then
            echo "ERROR: No model manifest found in karAQI-data. Run training pipeline first."
            exit 1
          fi
          echo "Model loaded from karAQI-data: $(ls -la models/aqi_forecast_hourly_ridge.joblib)"

      - name: Export pre-computed forecast (static JSON)
        run: python -m src.export_forecast --source store

      - name: Verify forecast file exists and is valid JSON
        run: |
          python -c "
          import json, sys
          with open('data/static_forecast.json') as f:
              data = json.load(f)
          n = len(data.get('outputs', []))
          print(f'Forecast OK: {n} outputs')
          if n != 30:
              print(f'ERROR: expected 30 outputs, got {n}', file=sys.stderr)
              sys.exit(1)
          "

      - name: Push forecast JSON to karAQI-data repo
        env:
          DATA_REPO_TOKEN: ${{ secrets.DATA_REPO_TOKEN }}
        run: |
          if [ -z "$DATA_REPO_TOKEN" ]; then
            echo "ERROR: DATA_REPO_TOKEN secret is not set. Cannot push to karAQI-data."
            echo "Set it at: https://github.com/faisaliqbalkhattak/karAQI/settings/secrets/actions"
            exit 1
          fi
          if [ ! -d /tmp/karAQI-data/.git ]; then
            echo "ERROR: karAQI-data checkout is missing from the model fetch step."
            exit 1
          fi
          mkdir -p /tmp/karAQI-data/data
          cp data/static_forecast.json /tmp/karAQI-data/data/
          cd /tmp/karAQI-data
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/static_forecast.json
          if git diff --cached --quiet; then
            echo "No changes to forecast JSON"
          else
            git commit -m "ci: refresh forecast JSON [skip ci]"
            git push
          fi
